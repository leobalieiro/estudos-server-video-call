version: '3.8'
services:
  app:
    container_name: server_video_call
    build: .
    ports:
      - "3000:3000"
      - "3001:3001"
    volumes:
      - ./src:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev
